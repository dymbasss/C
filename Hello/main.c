#include "/home/fox/tests_program/Hello/library/header.h"

#pragma region test
	// int N = 5;
	// int m[4];
	// int NS = sizeof(m) / sizeof(m[0]);

	// printf("NS: %d \n\n", sizeof(NS));
	// printf("m: %d \n\n", sizeof(m));

	// printf("int m[4]\n");
	// printf(" sizeof(m) is %lu byte\n", sizeof(m));
	// printf(" sizeof(&m) is %lu byte\n", sizeof(&m));
	// printf(" sizeof(*m) is %lu byte\n\n", sizeof(*m));


	// printf("  m is %d \n", m);
	// printf(" &m is %d \n", &m);
	// printf(" *m is %d \n\n", *m);

	// uint8_t *p = (uint8_t*)malloc(sizeof(uint8_t) *4);
	// uint N = sizeof(p) / sizeof(p[0]);
	// printf("p: %lu \n\n", N);

	// srand( (unsigned)time(NULL));
	// int lol[1];
	// int i = 0;
	// lol[i] = rand()%2 + 1;
	// lol[i++] = rand()%20;
	// printf("p: %d %d\n",lol[0], lol[1]);
	

	// printf("int *p = (int*)malloc(sizeof(int) * 4)\n");
	// printf(" sizeof(p) is %lu byte\n", sizeof(p));
	// printf(" sizeof(&p) is %lu byte\n", sizeof(&p));
	// printf(" sizeof(*p) is %lu byte\n\n", sizeof(*p));

	// printf("  p is %d \n", p);
	// printf(" &p is %d \n", &p);
	// printf(" *p is %d \n\n", *p);

	// int  n  = 6;    // Объявление переменной n типа int и присваивание ей значения 6
	// printf("%d\n", n);
	// int *pn = (int*)malloc( sizeof ( int ) ); // Объявление указателя pn и выделение под него памяти
	// printf("%d\n", *pn);
    // *pn = 5;    // Разыменование указателя и присваивание значения 5
	// printf("%d\n", *pn);
    // n = *pn;   // Присваивание n того значения (5), на которое указывает pn
	// printf("%d\n", n);
    // free(pn);  // Освобождение занятой памяти
	// printf("%d\n", *pn);
    // pn = &n;   // Присваивание указателю pn адреса переменной n (указатель будет ссылаться на n)
	// printf("%d\n", *pn);
    // n = 7;     // *pn тоже стало равно 7
	// printf("%d\n", n);
	// printf("%d\n", *pn);

	// uint8_t p[] = {0x30,0x31,0x30,0x31,0x30,0x31,0x30,0x31,0x30,0x31};
	// uint8_t N = sizeof(p) / sizeof(p[0]);
#pragma endregion